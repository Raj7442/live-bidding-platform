{
  "name": "live-bidding-frontend-vercel",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r build/* ../",
    "vercel-build": "cd frontend && npm install && npm run build"
  }
}